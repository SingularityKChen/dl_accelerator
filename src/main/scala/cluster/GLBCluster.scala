package dla.cluster

import chisel3._
import chisel3.util._

class GLBCluster(debug: Boolean) extends Module {
  val io = IO(new Bundle {

  })
}

class PSumSRAMBank extends Module {
  val io = IO(new Bundle {})
}

class IactSRAMBank extends Module {
  val io = IO(new Bundle {})
}
