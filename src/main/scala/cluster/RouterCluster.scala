package dla.cluster

import chisel3._
import chisel3.util._

class RouterCluster extends Module {
  val io = IO(new Bundle {

  })
}

class PSumRouter extends Module {
  val io = IO(new Bundle {})
}

class WeightRouter extends Module {
  val io = IO(new Bundle {})
}

class IactRouter extends Module {
  val io = IO(new Bundle {})
}